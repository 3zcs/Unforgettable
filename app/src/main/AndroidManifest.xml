<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unforgettable.azcs.me.unforgettable">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.main.MainActivity" />
        <activity
            android:name=".feature.show_word.ShowWordActivity"
            android:theme="@style/AppThemeWithoutActionBar" />
        <activity android:name=".feature.edit_word.EditWordActivity" />
        <activity android:name=".feature.add_word.AddWordActivity" />
        <activity
            android:name=".feature.authentication.AuthActivity"
            android:noHistory="true"
            android:theme="@style/AppThemeWithoutActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>